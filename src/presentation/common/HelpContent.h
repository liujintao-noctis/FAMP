#pragma once

#include <QString>

namespace famp::help
{
inline QString quickStartHtml()
{
    return QStringLiteral(R"HTML(
<h2>FAMP 快速入门</h2>
<ol>
  <li><b>管理项目：</b>使用“文件 → 新建/打开/保存项目”管理 `.famp` 点云项目；项目会保存统一内容树、点云原始坐标与变换、派生成果、控制点、考古字段、可见性、稳定二维图元 ID、二维/三维测量和窗口布局，未保存更改每 60 秒自动备份；源资产移动后，打开项目时可重新定位。</li>
  <li><b>坐标系与重投影：</b>使用“工具 → 项目坐标系”记录 EPSG 元数据；“坐标转换器”可用 PROJ 转换单个坐标点；选择点云后可按双精度变换和原点使用“点云局部/真实坐标”双向换算，或使用“重投影所选点云”逐点转换整云。重投影会无损保留已有逐点属性，结果默认作为内存点云加入内容树，需要时再另存含空间参考的 PCD，原图层不会被隐式替换。</li>
  <li><b>打开点云：</b>使用“文件 → 打开点云”选择 PCD、LAS、LAZ、PLY 或 XYZ 文件，也可将文件直接拖入主窗口；LAS/LAZ 会保留常用逐点属性，XYZ 接受 3 列坐标或 6 列坐标/RGB。读取会在后台逐个完成，期间仍可查看和操作已加载内容，并可从状态栏安全取消当前文件及剩余队列。</li>
  <li><b>继续工作：</b>“文件 → 最近打开”会保留最近 8 个可用点云文件。</li>
  <li><b>检查点云：</b>左侧内容树统一管理点云、分析、测量和二维图元；中间是 VTK 点云视口，右侧是默认等宽的考古制图画布。VTK 左下角的红 X、绿 Y、蓝 Z 方向轴会随相机旋转。内容树可分组、扩展选择、拖拽排序、重命名、切换可见性、锁定、删除、定位、查看属性或按类型另存。点云包围盒默认关闭；点击点云只显示当前点云包围盒，点击二维成果或三维测量时会清除包围盒并在对应视图高亮。</li>
  <li><b>显示、处理与配准：</b>在内容列表选择点云后，可从“工具”调整点大小、透明度，并切换 RGB、统一颜色、局部高程 Z 或 LAS/LAZ 逐点属性渐变（自动/手动范围），属性模式会显示类型、值数和范围；也可在后台执行体素降采样、统计离群点去噪，或按局部 X/Y/Z 范围保留内部/外部点。处理参数可保存/载入为 JSON 方案。加载至少两个点云后，可用“点云 ICP 配准”将源云对齐到目标云局部框架，并可用配准体素降低大点云耗时；ICP 要求初始位置足够接近，无有效重叠时不会输出结果。以上成果默认作为保留逐点属性和来源关系的内存点云加入内容树，需要时再另存含双精度空间参考的 PCD。取消操作不会写入或集成结果。</li>
  <li><b>控制点空间配准：</b>选择点云后使用“工具 → 控制点与空间配准”，填写点云局部坐标和同名实测目标坐标；至少启用 3 个不共线点即可解算保距 3D 刚体变换。界面会列出逐点总残差以及 RMSE、平均和最大残差；可只保存记录，也可应用变换。应用时已有三维测量会同步旋转并保持测量尺度，操作可撤销/重做；控制点和质量统计会写入项目与考古报告。</li>
  <li><b>DEM 与等高线：</b>选择未锁定点云后使用“工具 → DEM 与等高线”。程序按双精度真实坐标生成网格，支持自动/手动分辨率、最低/最高/平均/中位数单元统计、最多 3 格的封闭 NoData 小空洞填补，以及自动/手动等高距、基准高程和 0–3 次平滑。经纬度图层必须先重投影到投影坐标系；未声明 CRS 时必须确认 X/Y 为米制平面坐标。成果默认加入内容树，可选立即另存 `.famp-dem`、ASC、DEM/等高线 CSV、SVG，并可把等高线作为可撤销且随比例尺缩放的图元加入二维画布。后台任务可取消且不会修改原点云。</li>
  <li><b>挖填方与体积：</b>选择未锁定的当前地表点云后使用“工具 → 挖填方与体积”，与固定设计高程或已有 `.famp-dem` 参考地表比较。参考 DEM 模式在后台读取并采用其分辨率，严格检查 CRS、单位、原点对齐、重叠和 NoData。高差为“当前 - 参考”：正值是挖方，负值是填方，净体积为“挖方 - 填方”。结果默认加入内容树并显示红/蓝/灰降采样概览，可选立即另存 `.famp-volume`、汇总 CSV、逐格 CSV 和 SVG。经纬度图层必须先重投影，未声明 CRS 时必须确认 X/Y/Z 全部以米为单位。</li>
  <li><b>点云高程剖面：</b>选择并显示点云后使用“工具 → 点云高程剖面”，在中央三维点云依次左键拾取起点和终点，Esc 或右键取消。程序按双精度真实坐标提取线段走廊，计算沿线里程和带符号偏距，并按自定义间隔输出最低、最高、平均或中位数代表高程以及段内最低/最高包络；空白段不会被跨越连线。成果默认加入内容树并在程序中预览，可选立即另存 `.famp-profile`、采样段 CSV、原始点 CSV 和 SVG。经纬度图层必须先重投影，后台任务可取消且不会修改原点云。</li>
  <li><b>切割与点云准备：</b>选择点云后可创建与其实际范围自适应的水平、竖直或任意切割面；“开始切割”只生成预览，点击“确认”才把结果紧跟来源点云加入内容列表，原点云保持显示。也可选择任意预处理、范围裁切、ICP 或重投影成果继续制图。</li>
  <li><b>考古制图流程：</b>主界面始终显示“①选择点云 → ②点云准备 → ③选择制图类型 → ④投影预览 → ⑤自动绘图 → ⑥编辑与 A4 出图”。平面图可选俯视/XOY，剖面图可选 XOY、XOZ 或 YOZ。投影默认只在中央 VTK 视口临时预览，不落盘、不自动加入内容列表；弹窗可选“自动绘图”、“加入内容列表”或“关闭预览”，三枚按钮的具体作用在鼠标悬停时显示，弹窗结束后临时投影自动消失。</li>
  <li><b>二维制图：</b>必须先生成俯视二维图，再生成 XOZ/YOZ 剖面图；在俯视图尚未存在时，剖面预览中的“自动绘图”会保持禁用，弹窗和流程栏会用橙色粗体突出俯视优先步骤。俯视、XOZ、YOZ 生成前均可分别在 0°、顺时针 90°、180°、270°之间切换实时预览并确认；俯视图及其两条剖切线共用俯视角度，XOZ/YOZ 各自保存确认角度。调整剖面方向不会转动俯视锚点，也不改变固定方位：YOZ 始终在俯视图正上方，XOZ 始终在俯视图正右方。生成 XOZ/YOZ 时会按当前选中的原始或切割点云范围，自动在俯视图上生成对应剖面切割线，并将 YOZ/XOZ 分别对齐该线的中心轴。五项图元均进入左侧“二维制图”文件夹，保存项目后仍保持关联。切换 1:10、1:20、1:50、1:100 时三图与切割线联动缩放、保持对齐并自动规避重叠，内容过大时扩展画布而不改变比例尺。随后设置比例尺并按 A4/A3 实尺导出。</li>
  <li><b>测量成果：</b>使用“工具 → 测量距离/测量面积/测量角度”后，可在中央三维点云直接拾取点，也可在右侧二维制图画布选点。移动鼠标会实时预览节点、连线和数值；距离和面积用右键完成，二维画布也可双击完成；角度按边点—顶点—边点选择，Esc 可取消。二维与三维结果都会保存到项目和成果报告，并可撤销、重做或集中清除。隐藏点云图层不会删除已有三维测量，重新显示图层后测量标注和拾取功能一并恢复。</li>
  <li><b>考古图层记录与报告：</b>选择点云后使用“工具 → 考古图层属性”填写遗址、发掘区、探方/探沟、地层、遗迹编号、年代、日期、记录人、说明及自定义字段；修改可撤销/重做并随项目保存。使用“文件 → 导出考古项目报告”生成 PDF 或 HTML，按图层列出考古字段、控制点与配准残差，并汇总项目路径、坐标参考系、比例尺、点云文件/点数/原点/可见性，以及二维/三维距离、面积/周长和角度测量成果；三维成果同时列出关联图层 ID 和 CRS。</li>
  <li><b>导出成果：</b>使用“导出平面图”按精确毫米定义的 A4/A3/自定义纸张、横向/纵向和 150/254/300/508/600 DPI 导出 PDF、SVG、PNG 或 BMP；默认“保持当前制图比例尺（实尺，不缩放）”并包含最小间隔 1 mm、5 mm/10 mm 加粗的米格纸，也可选择明确标注为会缩放成果的“自动适合页面”，并在保存前打开 A4/A3 实尺打印预览。PDF/SVG 使用矢量毫米坐标；PNG/BMP 含米格纸时自动采用 254 或 508 DPI，使 1 mm 对应 10 或 20 像素。纸质打印还必须在打印机属性中选择“实际大小/100%”，关闭“适合页面/缩小超大页面”。屏幕米格纸会随窗口所在显示器的物理 DPI 自动重算；显示器物理尺寸上报错误时仍需用直尺校准确认。</li>
</ol>
<p>操作前请先在内容列表中选中目标点云。控制台会显示加载、切割、投影和保存结果。</p>
)HTML");
}

inline QString shortcutsHtml()
{
    return QStringLiteral(R"HTML(
<h2>键盘快捷键</h2>
<table cellspacing="8">
  <tr><th align="left">快捷键</th><th align="left">功能</th></tr>
  <tr><td>Ctrl+O</td><td>打开点云</td></tr>
  <tr><td>Ctrl+N</td><td>新建项目</td></tr>
  <tr><td>Ctrl+Shift+O</td><td>打开项目</td></tr>
  <tr><td>Ctrl+Shift+S</td><td>保存项目</td></tr>
  <tr><td>Ctrl+S</td><td>保存二维制图成果</td></tr>
  <tr><td>Ctrl+Z</td><td>撤销最近的制图、测量或项目操作</td></tr>
  <tr><td>Ctrl+Shift+Z / Ctrl+Y</td><td>重做最近撤销的操作</td></tr>
  <tr><td>Ctrl+F12</td><td>全屏显示</td></tr>
  <tr><td>Delete</td><td>删除选中的二维图元</td></tr>
  <tr><td>方向键</td><td>移动选中的二维图元</td></tr>
  <tr><td>Ctrl+Shift+Left/Right</td><td>逆时针/顺时针旋转选中的二维图元</td></tr>
  <tr><td>Ctrl+Alt+D / Ctrl+Alt+A / Ctrl+Alt+G</td><td>测量三维点云或二维画布的距离/面积/角度</td></tr>
  <tr><td>F1</td><td>打开快速入门</td></tr>
</table>
<p>双击文字图元可以修改字体。拖动图元边界或控制点可以完成移动和旋转。</p>
)HTML");
}

inline QString aboutHtml(const QString& applicationVersion,
                         const QString& qtVersion,
                         const QString& vtkVersion,
                         const QString& pclVersion,
                         const QString& projVersion)
{
    return QStringLiteral(R"HTML(
<h2>FAMP %1</h2>
<p>Field Archaeology Mapping Program</p>
<p>用于点云查看、切割、投影和田野考古二维制图。</p>
<table cellspacing="8">
  <tr><td><b>Qt</b></td><td>%2</td></tr>
  <tr><td><b>VTK</b></td><td>%3</td></tr>
  <tr><td><b>PCL</b></td><td>%4</td></tr>
  <tr><td><b>PROJ</b></td><td>%5</td></tr>
</table>
<p><a href="https://github.com/liujintao-noctis/FAMP">GitHub 项目主页</a></p>
)HTML").arg(applicationVersion.toHtmlEscaped(),
             qtVersion.toHtmlEscaped(),
             vtkVersion.toHtmlEscaped(),
             pclVersion.toHtmlEscaped(),
             projVersion.toHtmlEscaped());
}
}
